[project]
name = "rsshub-mcp"
version = "0.1.0"
description = "Model Context Protocol Support for RSSHub"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cachetools>=5.5.2",
    "fastmcp>=2.2.6",
    "httpx>=0.28.1",
    "pydantic>=2.11.4",
]
